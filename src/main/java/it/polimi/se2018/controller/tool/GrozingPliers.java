package it.polimi.se2018.controller.tool;

import it.polimi.se2018.model.*;

public class GrozingPliers extends Tool {

    public void use(Match match, Player player) {

        // Show dice in DraftPool -> show(match.getDraftPool())
        // Ask for a Die
        // Ask if increase or decrease
        // int val = match.getDraftPool().get(n).getValue();
        // if val==6 and scelta==increase -> return
        // if val==1 and scelta==decrease -> return
        // match.getDraftPool().get(n).setValue(n+-1) according to choice

    }

}
