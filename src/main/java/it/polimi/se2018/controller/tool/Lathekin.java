package it.polimi.se2018.controller.tool;

import it.polimi.se2018.model.*;

public class Lathekin extends Tool {
    public void use(Match match, Player player) {
        // show windows pattern of the player
        // ask for the dice
        // ask for the position
        // if verifyAllRestrictions() = true then   --> method which verifies restrictions. Maybe in controller?
        // player.getWindowPattern.setDie(x, y)
        // and another one...
    }
}
