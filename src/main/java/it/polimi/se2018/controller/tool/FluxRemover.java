package it.polimi.se2018.controller.tool;

import it.polimi.se2018.model.*;

public class FluxRemover extends Tool {
    public void use(Match match, Player player) {
        // choose a die from the draftpool
        // draftPool.removeDie(die)
        // draftPool.addDie(match.bag.extractDie());
        // die.setValue(scelta);
        // if wanted -> position i t
    }
}
